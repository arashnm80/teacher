**Page 46**

### Assessment of data modeling competency

Performance standard: The student should be able to define and manage data in Python, perform basic operations on data, use conditional structures and loops, and process data in an organized way.

Score

Conditions for performing the work (tools, materials, equipment,

Tools

Results

Row

Steps

Standard (indicators / judgment / scoring)

Earned

time, place, etc.)

### Assessment

Enabled

Installing Python and a suitable development environment (`IDE`) on a personal computer or using online environments. Running simple programs in the `IDLE` environment Writing the first `print`() statement Understanding the difference between the command line and running a file.

3

Receiving a piece of Python code with syntax or indentation errors and correcting it.

Doing the work with other methods Doing the Try this activities

Basics and getting started with

Computer, `vscode` installed, browser, 15 minutes, observation

1 Running simple programs in the `.IDLE` environment

1

Python

Workshop

Practical

2 Writing the first `print`() statement

3 Understanding the difference between the command line and running a file.

2

4 Receiving a piece of Python code with syntax or indentation errors and correcting it.

Not doing or incompletely doing work step 1 Writing code in which several variables with different data types (string, integer, decimal number) are defined and their values are used later. Writing code that converts one data type to another and uses it in calculations.

3

Answering multiple-choice or open-ended questions about correct variable naming, differences among data types, and when to use type conversion. 4 Doing the work with other methods Doing the Try this activities

Variables, data types and

Computer, `vscode` installed, practice file,

Observation

2

type conversion

15 minutes

Practical Writing code in which several variables with different data types (string, integer, decimal number) are defined and their values are used later. Writing code that converts one data type to another and uses it in calculations

2

. Answering multiple-choice or open-ended questions about correct variable naming, differences among data types, and when to use type conversion.

Not doing or incompletely doing work step 1 Writing a program that uses arithmetic operators (addition, subtraction, multiplication, division) and perhaps comparison operators.

Writing a program that receives information from the user and then displays a customized output.

3

Solving a simple problem that requires combining input, processing with operators, and displaying output (for example calculating the area of a rectangle by receiving length and width from the user).

Doing the work with other methods Doing the Try this activities Observation Writing a program that uses arithmetic operators (addition, subtraction, multiplication, division) and

3 Operators and input/output

Computer, `vscode` installed, 15 minutes

Practical perhaps comparison operators. Writing a program that receives information from the user and then displays a customized output. Solving a simple problem

2

that requires combining input, processing with operators, and displaying output (for example calculating the area of a rectangle by receiving length and width from the user).

Not doing or incompletely doing work step 1


---

**Page 47**

Creating a list, adding a new item to it, deleting an item, and displaying the list contents. Writing code using `else`, `elif`, `if` so that a decision is made.

Combining conditions using `and`, `or`, `not` to implement more

3

complex logic.

Building a simple program that combines a list and a condition Doing the Try this activities Doing the work with other methods

Basic data structures

Observation

4

Computer, `vscode` installed, 20 minutes

(list) and conditional

Practical Creating a list, adding a new item to it, deleting an item and displaying the list contents Writing code using `else`, `elif`, `if` so that a decision is made.

2

Combining conditions using `and`, `or`, `not` to implement more complex logic.

Building a simple program that combines a list and a condition.

Not doing or incompletely doing work step 1 Writing code that traverses the elements of a list or the characters of a string and prints each one. Using the `range`() function in a `for` loop to run a statement a specific number of times or to traverse numbers in a range.

3

Writing a program that uses a `for` loop to do a repetitive task Doing the Try this activities Doing the work in several ways Observation

5

Repetition loops: `for`

Computer, `vscode` installed, 25 minutes

Practical Writing code that traverses the elements of a list or the characters of a string and prints each one. Using the `range`() function in a `for` loop to

2

run a statement a specific number of times or to traverse numbers in a range.

Writing a program that uses a `for` loop to do a repetitive task Not doing or incompletely doing work step 1 Writing a program that continues as long as a condition is met Writing code in which `break` is used to exit the loop and `continue` is used to skip a particular repetition. Solving a problem that requires

3

smartly combining loops (`for` or `while`) and conditional statements. Doing the Try this activities Doing the work in several ways

Repetition loops and control

Observation

6

Computer, `vscode` installed, 25 minutes

statements `while`

Practical

1 Writing a program that continues as long as a condition is met Writing code in which `break` is used to exit the loop and `continue` is used to skip

2

a particular repetition. Solving a problem that requires smartly combining loops (`for` or `while`) and conditional statements.

Not doing or incompletely doing work step 1

Non-technical competencies, safety,

Meeting all non-technical competencies 2

hygiene, environmental considerations and attitude:

Not meeting any of the non-technical competencies 1 Yes

Work assessment (competency in performing the work)

No

### Explanation:

1 Criterion for competency in performing the work:

Earning a minimum score of 2 from steps 1, 2 and 3 (critical steps) Earning a minimum score of 2 from the section on non-technical competencies, safety, hygiene, environmental considerations and attitude Earning a minimum average of 2 from the work steps

2 Definition of competency levels: Regardless of the level of professional qualification at which a work task is performed, in any work environment a specific quality of performing each task may be expected. The level of competency in performing the work is the basic criterion for assessment.

Skill (level three): skilled and able to train and guide others, ability to plan and analyze, accountability for one's own work, dealing with a wide range of tasks and activities Mastery (level four): expertise in performing the work and training others, creating innovation, adaptability, troubleshooting, leading and guiding others.
