**Page 80**

### Assessment of competence in working with functions and modules

Performance standard: The student should be able to create and use functions and modules, manage data and repetitive operations with functions, make use of local modules, and recognize the difference between a function and a method.

Score

Conditions for carrying out the work (tools, materials, equipment,

Tools

Results

Row

Steps

Standard (indicators/judgment/scoring)

Earned

time, place, and so on)

### Assessment

made possible

Build a `set` with several arbitrary elements. Add a new element to the `set`.

Remove an element from the `set` (both an element that exists and an element that does not, so you see a possible error). Check whether a particular element exists in the `set` or not. Build an empty `dictionary`. Know the main difference between a `set` and a

3

`list` or `tuple` (for example, lack of order and duplicate elements).

Know the use of a `set` when you need to remove duplicates. Explain the key–value (`key` `value`) feature in a `dictionary`. Carry out Try this activities by observation, practical,

1

Sets (`Set`)

Computer, `vscode` installed, 15 minutes

Check Build a `set` with several arbitrary elements. Add a new element to the `set`.

List Remove an element from the `set` (both an element that exists and an element that does not, so you see a possible error). Check whether a particular element

2

exists in the `set` or not. Build an empty `dictionary`. Know the main difference between a `set` and a `list` or `tuple` (for example, lack of order and duplicate elements).

Familiarity with definitions at the level of explaining concepts Incomplete completion of steps 1 Build a `dictionary` with several key–value pairs. Add a new key–value pair to the `dictionary`. Remove an element from the `dictionary` based on its key. Try to remove an element with a nonexistent key so you encounter an error and explain the reason. Know the difference between a key (`key`) and a value (`value`) in a

3

`dictionary`. Explain the importance of keys being unique in a `dictionary`. Describe methods of accessing values in a `dictionary` (using the key). Carry out Try this activities by observation

Computer, `vscode` installed, practice file,

Practical,

2

`Dictionary` data structure

Build a `dictionary` with several key–value pairs. Add a new key–value pair

15 minutes

Check to the `dictionary`. Remove an element from the `dictionary` based on its key. List Try to remove an element with a nonexistent key so you encounter an error

2

and explain the reason. Know the difference between a key (`key` and a value (`value` in a `dictionary`. Familiarity with definitions and explanation Incomplete completion of steps 1 Build a `tuple` with several elements (for example, the coordinates of a point or information about a person). Access the elements of the `tuple` using an index. Try to add or remove an element from the `tuple` and observe the result (error) and state the reason (optional). Try to delete a `tuple`.

3

Know the main feature of a `tuple` that distinguishes it from a `list`? Referring to immutability or `Immutability`. In what cases is using a `tuple` preferable to a `list`? (for example, when observation data must not change, or as a key in a `dictionary`) Carry out Try this activities practical,

3

`Tuple` data structure

Computer, `vscode` installed, 20 minutes

Question Build a `tuple` with several elements (for example, the coordinates of a point or information about a person) oral Access the elements of the `tuple` using an index. Try to add or remove an element from the `tuple` and observe the result (error) and try

2

to delete a `tuple`.

Know the main feature of a `tuple` that distinguishes it from a `list`.

Familiarity with definitions at the level of explaining Incomplete completion of steps 1


---

**Page 81**

Write a simple function that only prints a welcome message. Write a function that takes two numbers as input (parameters) and prints their sum. Call this function with different values (arguments).

3

Know why functions are used in programming? Explain the difference between a “parameter” (function definition) and an “argument” (the value passed when calling). Observation Carry out Try this activities

```
توابع (Functions)
```

Practical,

4

Computer, `vscode` installed, 20 minutes

Basics and parameters

Submit Write a simple function that only prints a welcome message. Write a function file that takes two numbers as input (parameters) and prints their sum

2

. Call this function with different values (arguments).

Know why functions are used in programming?

Familiarity with definitions at the level of explaining Incomplete completion of steps 1 Write a function that takes two numbers as input, calculates their product, and returns it. Store the function’s return value in a variable and then print that variable. Write a function that takes a list of numbers as input and, using

3

a loop, calculates and returns the sum of the list elements. Use the `sorted` function to sort a list and see the result. Explain the difference between a function that `print`s a value and a function that `return`s a value, observation Explain the role of the `return` statement in functions. Carry out Try this activities

```
توابع (Functions)
```

Practical,

5

Computer, `vscode` installed, 10 minutes

Return value and uses

Question Write a function that takes two numbers as input, calculates their product oral and returns it. Store the function’s return value in a variable and then print that variable. Write a function that takes a list of numbers as

2

input and, using a loop, calculates and returns the sum of the list elements. Use the `sorted` function to sort a list and see the result. Familiarity with definitions at the level of explaining Incomplete completion of steps 1 Create a simple module (for example, a Python file with several functions) in the same folder. Run the main file and call the function defined in the local module using `import`. Use a standard Python module such as `math` (for example `import` `math` and then `math` `.sqrt`() or `math.pi`). (Optional)

3

Name a third-party module that has already been installed. Explain the purpose of using modules. Explain the main difference between a “function” and a “method” (`method`), observation Carry out Try this activities

Modules (`Modules`) and

Practical,

6

Computer, `vscode` installed, 20 minutes

Differences

Question

1 Create a simple module (for example, a Python file with several functions) oral in the same folder. Run the main file and call the function defined in the local module

2

using `import`. Use a standard Python module such as `math`.

Familiarity with definitions at the level of explaining Incomplete completion of steps 1

Non-technical competencies, safety,

Verification of all non-technical competencies 2

hygiene, environmental considerations, and attitude:

Failure to verify any of the non-technical competencies 1 Yes

Work assessment (competence in performing the work)

No

### Explanation:

1 Criterion for competence in performing the work:

Earning at least a score of 2 from steps 1, 2, 3, 4, 5, and 6 (critical steps) Earning at least a score of 2 from the section on non-technical competencies, safety, hygiene, environmental considerations, and attitude Earning at least an average of 2 from the work steps

2 Definition of competence levels: Regardless of the level of professional qualification at which a work task is performed, in every workplace a specific quality of work may be expected. The level of competence in performing the work is the basic criterion for assessment.

Skill (level three): Skilled and able to train and guide others, ability to plan and analyze, accountability for one’s own work, dealing with a wide range of tasks and activities Mastery (level four): Expertise in performing the work and training others, creating innovation, adaptability, troubleshooting, leading and guiding others.


---

**Page 82**
