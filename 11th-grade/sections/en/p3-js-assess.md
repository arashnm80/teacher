**Page 231**

### Assessment of competency in designing interactive web pages with JavaScript

Performance standard: The student must be able to use `JavaScript` optimally and securely to add dynamic and interactive behaviors to web pages and run the final output in various browsers without error.

Score

Conditions for performing the work (tools, materials, equipment,

Tools

Results

Row

### Steps

Standard (indicators / judgment / scoring)

Obtained

time, place, and so on)

### Assessment

Enabled

Correct definition of JavaScript and its application on the web; recognizing the difference between `JavaScript` and `HTML` and `CSS`; explaining methods of adding (`JS` `inline`, internal, external); creating a `js` file and connecting it correctly to `HTML`

3

Using `console.log` without error; running the script in the browser; doing the Try this activities; performing the steps with other methods; observation

Familiarization, setup, and getting started

Practical

1

Computer, `IDE`, browser, 30 minutes, workshop

Creating a `js` file and connecting it correctly to `HTML`; using `console.log` without

Coding

Oral questioning; error; running the script in the browser 2

Only defining JavaScript and its application on the web; recognizing the difference between `JavaScript` and `HTML` and `CSS`; explaining methods of adding (`inline.Js`, internal, external 1)

Defining a variable with `let`; recognizing data types; explaining the difference between == and ===; recognizing logical and arithmetic operators; type conversion

```
(Number, String)
```

3

Performing calculations and comparisons; doing the Try this activities; performing the steps with other methods; practical observation

2 Defining and using variables

Computer, `IDE`, browser, 10 minutes

Oral questioning: defining a variable with `let` and `const`; recognizing data types; explaining the difference between == and ===; recognizing logical and arithmetic operators; type conversion

2

(`Number`, `String`); performing calculations and comparisons; defining a variable with `let`; recognizing data types; incomplete performance of the steps 1 Defining an array and an object; recognizing the difference between an array and an object; recognizing how to access elements; creating a one-dimensional and a two-dimensional array; creating an object with several properties; accessing and changing element values; building a products array and displaying its information

3

Creating a student object with several properties and displaying it; iterating over an array with a loop; doing the Try this activities; performing the steps with other methods; practical observation

3 Arrays and objects

Computer, `IDE`, browser, 15 minutes

Oral questioning: creating a one-dimensional array; creating an object with several properties; accessing and changing element values; building a products array and displaying its information; a student object with

2

several properties and displaying it; iterating with a loop; defining an array and an object; recognizing the difference between an array and an object; recognizing how to access elements; creating a one-dimensional array; incomplete performance of the steps 1

Explaining the use of `if`; explaining the difference between the `for` and `while` loops; recognizing `break` and `continue`; correct use of `if/else`; using a loop

3

`while` ,`for`; iterating over an array with `for`…`of`; doing the Try this activities; performing the steps with other methods; observation

Conditional structures and

Practical

4

Computer, `IDE`, browser, 15 minutes

loops

Oral questioning: explaining the use of `if`; explaining the use of `for` and `while`; recognizing `break` and `continue`; using `while` ,`for` ,`if`; iterating over an array with 2 `for`…`of`

Incomplete performance of the steps 1


---

**Page 232**

Defining a function; difference between a parameter and an argument; recognizing the built-in functions `Math` and `String` and `Array`; creating a function with a parameter; calling a function; using the methods `Math.random`() `toUpperCase`()

3

`pop` ,)(`push`(); writing a function to calculate the average; using a function for observation of input validation; using array methods in a practical example; practical performance, Try this activities; performing the steps with other methods

5 Functions and built-in functions

Computer, `IDE`, browser, 15 minutes

Oral questioning: defining a function; using mathematical and string functions 2 Incomplete performance of the steps or knowledge limited to definition 1 Defining the concept of `DOM`; explaining the structure of `document`; defining a class and an object; creating a class and instantiating; selecting elements with: `getElementById` `quer`

```
ySelector
```

Changing the content and style of elements; creating a button that when clicked

3

changes the page text; creating a simple form and receiving the input value; managing events (`event` `handling`) without error; observation; doing the Try this activities; performing the steps with other methods

Classes and `DOM`; interacting with

Practical,

6

Computer, `IDE`, browser, 15 minutes

the web page

Oral questioning: defining the concept of `DOM`; explaining the structure of `document`; defining a class and an object; creating a class and instantiating; selecting elements with: `getElementById` `quer`

2

```
ySelector
```

Defining the concept of `DOM`; explaining the structure of `document`; defining a class and an object; incomplete performance of the steps 1 Achieving all non-technical competencies ٢

Non-technical competencies, safety, health, environmental considerations, and attitude:

Failure to achieve any of the non-technical competencies ١ Yes

Work assessment (competency in performing the work)

No

### Explanation:

1 Criterion for competency in performing the work:

Obtaining at least a score of 2 from steps 1, 5, and 6 (critical steps); obtaining at least a score of 2 from the section on non-technical competencies, safety, health, environmental considerations, and attitude; obtaining at least an average of 2 from the work steps

2 Definition of competency levels: Regardless of the level of professional qualification at which a work task is performed, in every workplace a specific quality of performing each task may be expected. The level of competency in performing the work is the basic criterion of assessment.

Skill (level three): Skilled and able to teach and guide others; ability to plan and analyze; accountability for one’s own work; dealing with a wide range of tasks and activities. Mastery (level four): Expertise in performing the work and teaching others; creating innovation; adaptability; troubleshooting; guiding and advising others.
